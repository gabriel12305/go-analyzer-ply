package main

func main() {

    a += 5
    b -= 3
    c *= 2
    d /= 4

    if a == b {
    }

    if a != b {
    }

    if a >= b {
    }

    if a <= b {
    }

    resultado := (a > b) && (c < d);
	valor := 10 % 3

	activo := true
	admin := false

	if activo || admin {
	}

	if !activo {
	}
    @
	arreglo := [3]int{1,2,3}

	fmt.Println(arreglo[0])
}